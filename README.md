# Мероприятия для организованных групп в Библиотеке книжных героев
[Открыть vbkg.pr-cbs.ru](https://vbkg.pr-cbs.ru "Мероприятия для организованных групп в Библиотеке книжных героев")

# Автоматическая сертификация с помощью acme-companion 
## Docker

### Docker-compose
- Добавлен контейнер с acme-companion
- Nginx заменён на nginx-proxy (для работы с acme-companion)
- В контейнере library-events добавлены переменные окружения:
      * ```VIRTUAL_HOST``` - определение домена для express
      * ```LETSENCRYPT_HOST``` - домены для сертификации
      * ```LETSENCRYPT_EMAIL``` - email для сертификации


## Структура

> В отличии от предыдущей версии внесены следующие изменения в структуру проекта:
> - Файлы, связанные с express server перенесены в vbkg-server
> - Файлы nginx-proxy перенесены в nginx-proxy
> - Конфигурация nginx-proxy находится в nginx-proxy/conf.d/nginx.conf

### Структура проекта
```
project/
├─ .env.example
├─ docker-compose.yml
├─ vbkg_server/
│  ├─ vbkg.Dockerfile
│  ├─ vbkg-pr-cbs-ru.conf
│  ├─ package.json
│  ├─ package-lock.json
│  ├─ data/
│  │  └─ db.json
│  ├─ public/
│  │  └─ ***
│  └─ server/
│     └─ ***
└─ nginx-proxy/
   ├─ nginx-proxy.Dockerfile
   └─ conf.d/
      └─ nginx.conf
```

### Структура на хосте
```
$HOME/
├─ vbkg/
│  ├─ docker-compose.yml
│  ├─ vbkg_server/
│  │  ├─ vbkg.Dockerfile
│  │  ├─ package.json
│  │  ├─ package-lock.json
│  │  ├─ data/
│  │  │  └─ db.json
│  │  ├─ public/
│  │  │  └─ ***
│  │  └─ server/
│  │     └─ ***
│  └─ nginx-proxy/
│     ├─ nginx-proxy.Dockerfile
│     └─ conf.d/
└─ vbkg_backup/
   └─ backup_*TIMESTAMP*.tar.gz.enc
```
После развёртывания проекта конфигурация ```vbkg-pr-cbs-ru.conf```, ```nginx.conf``` и ```.env``` удаляются.

## CI/CD

> **Важно!**
> При развётрывании проекта в продакшен, замените значения secrets на реальные.

### Логика развёртывания:
0. Установка необходимых для работы пакетов __Sourcecraft__.
1. Настройка SSH и known hosts.
* При возникновении ошибки, в последующих шагах создаётся файл ```failure.reason``` с именем шага, на котором возникла ошибка.
2. Синхронизация файлов с репозиторием (rsync).
   * Исключаются файлы: 
      * ```.git```;
      * ```.gitignore```;
      * ```.sourcecraft```;
      * ```.env.example```;
      * ```README.md```.
* Последующие шаги происходят через shh сессии.
3. Настройка ```.env```.
   1. Файл ```.env``` заполняется с помощью функции ```update_env_var```.
   2. Пример использования: ```update_env_var "<variable name>" "<value>"```.
4. Остановка существующего контейнера и запуск нового.
5. Если развертывание прошло успешно то:
   1. Создаем новый бэкап.
   2. Удаляем старый бэкап.
6. При возникновении ошибки:
   1. Удаляем новый бэкап.
   2. Откатываемся к предыдущей версии.
   3. На сервер отправляется файл с причиной ошибки ```failure.reason```.
7. Удаление файлов конфигурации ```express```, ```nginx-proxy``` и ```.env```



# Технические заметки разработчика проекта до внедрения автоматической сертификации

убрали доступ к поисковым системам.
теперь БД хранится в папке \server


в .env
```
EMAIL_USER= ***почта, куда отправлять письмо*** 
EMAIL_PASS= ***пароль приложения (генериться в почтовом сервисе)***
ADMIN_EMAIL= ***почта админа, откуда шлётся письмо***
FRONTEND_URL=http://localhost:3000 (используется в ***app.use(cors)*** и в ***const PORT = process.env.PORT || 3000;***  на сервере.
```

### server.js

В файле server.js строчка ```const adapter = new FileSync('db.json');``` 

должна поменяться на такую: 

```const adapter = new FileSync('./data/db.json');```

В этом же файле после строчки 
```app.use(express.static(path.join(__dirname, '../public')));```

добавляется строчка:
```app.set('trust proxy',true);```

Если меняется почтовый сервис (гугл, яндекс и тд), то в ```const emailTransporter = nodemailer.createTransport``` сервера нужно изменить имя сервиса

Сервер автоматически бронирует мероприятие, чтобы не случилось, что мероприятие забронировали за 5 минут до начала. В 11 и 12 строке server.js меняется периодичность проверки автоматического бронирования. В 13 строке - периодичность создания бэкапа

```const checktime```

```const AUTO_BOOK_DAYS```

```const backtime```

Сервер проверяет количество созданных мероприятий и удаляет лишние, чтобы не перегружать БД и клиента (т.к. клиент получает всю БД). Сейчас проверка стоит каждый месяц: если больше 1200 мероприятий, удаляет 300 (и связанные бронирования). При этом делается бэкап БД. Должно хватить на пару-тройку лет (из расчета, что каждую неделю проходит 10 мероприятий - на самом деле меньше)...

### const CLEANUP_CONFIG

При использовании старой БД автоматом проставляется, что мероприятие платное (500 руб).

При удалении картинки, она удаляется из БД, но остаётся в ```server\uploads``` в ```app.delete('/api/events/')``` можно настроить удаление файла из папки.

### script.js

```function compressImage``` нужна для сжатия изображений и сохранения их в .webp. Задана максимальная ширина 500px.